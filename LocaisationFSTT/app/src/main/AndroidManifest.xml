<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/_209957"
        android:label="@string/app_name"
        android:roundIcon="@drawable/_209957"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetDevMobile"
        tools:targetApi="31">

        <meta-data
            android:name="com.mapbox.EnableEvents"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".ActiviteInscription"
            android:exported="false" />
        <activity
            android:name=".ActiviteConnexion"
            android:exported="false" />
        <activity
            android:name=".splashActviity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>